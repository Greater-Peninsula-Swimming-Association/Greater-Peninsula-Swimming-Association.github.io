# .github/workflows/build-rulebook.yml
name: Build GPSA Rulebook PDF

on:
  push:
    paths:
      - 'dev-docs/rulebook/**'
      - 'dev-docs/_template/**'
  workflow_dispatch:

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          echo "Building custom Docker image..."
          docker build -t gpsa-doc-builder dev-docs/

      - name: Build GPSA Rulebook PDF
        run: |
          echo "Building GPSA Rulebook PDF..."
          docker run --rm \
            -v "$(pwd)":/work \
            --workdir /work \
            gpsa-doc-builder /bin/sh -c "
              pandoc --template=dev-docs/_template/template.tex \
                -s dev-docs/rulebook/rulebook.md \
                -o dev-docs/rulebook/GPSA_Rulebook.pdf \
                --number-sections \
                --toc
            "
          echo "Build completed!"

      - name: Verify PDF was created
        run: |
          if [ -f "dev-docs/rulebook/GPSA_Rulebook.pdf" ]; then
            ls -lh dev-docs/rulebook/GPSA_Rulebook.pdf
            echo "✅ PDF successfully generated: dev-docs/rulebook/GPSA_Rulebook.pdf"
          else
            echo "❌ Error: dev-docs/rulebook/GPSA_Rulebook.pdf was not created"
            exit 1
          fi

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: GPSA-Rulebook
          path: dev-docs/rulebook/GPSA_Rulebook.pdf
          retention-days: 90

      - name: Commit PDF to repository
        if: github.event_name == 'push'
        run: |
          echo "Configuring git..."
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          echo "Staging PDF file..."
          git add dev-docs/rulebook/GPSA_Rulebook.pdf

          if git diff --staged --quiet; then
            echo "✅ No changes to commit (PDF unchanged)"
          else
            echo "📝 Committing updated PDF..."
            git commit -m "Update GPSA Rulebook PDF [skip ci]"

            echo "⬆️  Pushing to repository..."
            git push

            echo "✅ PDF committed and pushed successfully"
          fi