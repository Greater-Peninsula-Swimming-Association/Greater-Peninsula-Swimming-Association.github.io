% LaTeX header customizations for GPSA Rulebook

% Required packages for tables
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

% Set chapter counter to start at 1 instead of 0
\setcounter{chapter}{0}

% Ensure proper counter behavior
\setcounter{secnumdepth}{3}

% Custom GPSA branding colors
\definecolor{GPSANavy}{HTML}{002366}
\definecolor{GPSARed}{HTML}{d9242b}
